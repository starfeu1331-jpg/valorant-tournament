'use client'

import * as React from 'react'

export interface ToasterProps {}

export function Toaster({}: ToasterProps) {
  return null // Implémentation simple, peut être enrichie avec react-hot-toast
}
